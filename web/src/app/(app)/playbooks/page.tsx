export default function PlaybooksPage() {
  return (
    <div
      style={{
        minHeight: "100%",
        padding: 24,
        color: "var(--text)",
        fontFamily: "DM Sans, sans-serif",
      }}
    >
      <h1
        style={{
          fontFamily: "Sora, sans-serif",
          fontSize: 20,
          fontWeight: 600,
          marginBottom: 8,
        }}
      >
        Playbooks
      </h1>
      <p style={{ fontSize: 13, color: "var(--text2)" }}>
        Industry automation packs — activate in one click.
      </p>
    </div>
  );
}

